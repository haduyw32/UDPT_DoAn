<html>
    <head>
        <meta charset="utf-8">
        <link href="css/default.css" rel="stylesheet" type="text/css">
        <link href="css/box.css" rel="stylesheet" type="text/css">
        <title>Home</title>
        <script src="js/jquery-1.9.1.min.js"></script>
    
    <% if @wuser? : %> //save cookie
    <script type="text/javascript">
        var _id = "<%-@wuser %>";
        var pass = "<%-@wpass %>";
        var name = "<%-@wname %>";
        var arrFriend = [];//{_id, name}
        var fcus;
        function setCookie(cname, cvalue) {
            document.cookie = cname + "=" + cvalue + ";";
        }
        $(document).ready(function() {
            setCookie("c_id", _id);
            setCookie("cpass", pass);
            setCookie("cname", name);
            window.location.href="/";
        });
    </script>
    <% else : %>
        <script src="js/Cookie.js"></script>
    <% end %>
    
    <% include 'header.ect' %>
    </head>
    <body>
        <div class="alert-box notice"><span>notice: </span>thông báo.
        <a href="#"> <img src="/images/error.png" align="right"> </a>
        </div>
        <script type="text/javascript">
            $(".alert-box img").click(function () { 
                $(".alert-box").slideUp();
            });
            $(".alert-box").slideDown();
        </script>

        <form name="func-form" class="header" action="\register" method="post" style="margin-top: 20px;">
            <div>
                <input type="submit" name="submit" value="Đăng kí tiêm" class="butFunc" style="margin-left: 38%"/>
            </div>
        </form>

        <form name="func-form" class="header" action="\review" method="get" style="margin-top: 20px;">
            <div>
                <input type="submit" name="submit" value="Xem lịch sử đăng kí" class="butFunc" style="margin-left: 35%"/></a>
            </div>
        </form>

        <script type="text/javascript">
            $("form div").ready(function() {
                var strhtml = "<input type=hidden name=user value=" +  getCookie("c_id") + " />";
                $("form div").append(strhtml);
            });
            
        </script>
    </body>
</html>

